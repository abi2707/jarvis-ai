services:
  - type: web
    name: jarvis-server
    runtime: node
    region: singapore
    plan: free
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 10000
